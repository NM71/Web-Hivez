function createCard(title, cname, views, monthsOld, duration, thumbnail) {
    // Finish this

    // inputs
    // Title, channel name, no.of views,
}


createCard("Web Dev Video 2", "Code with me", 560000, 7, "12:36", "")






let card = document.createElement("div");
card.innerHTML = ""

document.querySelector(".container").after(card)